# 画面マップ

## 画面一覧

| 画面名 | パス | ファイル | 概要 | 状態 |
|--------|------|----------|------|------|
| ホーム | `/` | `src/pages/Home.jsx` | 今日の予定・やること・おすすめ・AI相談 | 実装中 |
| 本棚 | `/bookshelf` | `src/pages/Bookshelf.jsx` | 読んだ本・お気に入り・読みたい・ランキング | 実装中 |
| 絵本詳細 | `/bookshelf/:id` | `src/pages/BookDetail.jsx` | 絵本の詳細・読み記録・AIレポート | 実装中 |
| 記録入力 | `/record` | `src/pages/Record.jsx` | 絵本読み聞かせの記録入力 | 実装中 |
| カレンダー | `/calendar` | `src/pages/Calendar.jsx` | 月カレンダー・行事管理 | 実装中 |
| AI相談 | `/ai` | `src/pages/AI.jsx` | AI絵本相談・検索（独立画面） | **将来実装** |
| マイページ | `/mypage` | `src/pages/MyPage.jsx` | 設定・クラス情報・シフト | 実装中 |

## ナビゲーション構成

```
BottomNav（現時点）
  ├── ホーム
  ├── 本棚
  ├── 記録（中央ボタン）
  ├── カレンダー
  └── マイページ
```

> **注意**：AI相談（`/ai`）は現時点ではBottomNavに追加しない。
> ホーム画面内のインラインセクションとして提供し、独立画面への遷移は将来対応とする。

## 画面遷移メモ

- ホーム → AI相談検索（ホーム画面内でインライン展開。`/ai` への遷移はしない）
- ホーム → 絵本詳細（最近の記録カードタップ）
- 本棚 → 絵本詳細（カードタップ）
- ホーム → 本棚（「本棚を見る」リンク）
- カレンダー ← シフト・行事の登録（将来実装）

## 将来の画面追加候補

- `/ai`：AI相談の独立画面（BottomNavへの追加も含めて検討）
- レポート画面：月次の育ちレポート・クラス傾向
